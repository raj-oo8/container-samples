# azure.yaml

# Add the schema URL for Azure Developer CLI
# Note: Replace the URL with the correct schema URL if available
# As of now, there is no official schema URL for azure.yaml, but you can use a placeholder or custom schema if needed

name: AspireApp
services:
  api:
    project: src/Aspire/Aspire.ApiService
    language: dotnet
    target: azure-container-apps
    ingress: internal
  web:
    project: src/Aspire/Aspire.Web
    language: dotnet
    target: azure-container-apps
    ingress: external